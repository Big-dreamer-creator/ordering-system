# AGENTS.md

## 1. 项目定位

这是一个用于课程设计和课程答辩的简易点餐系统。

项目的核心目标：

- 功能完整但保持简单
- 前后端结构清晰
- 数据库结构清晰
- API 接口清晰
- 能够完整运行
- 代码容易理解和讲解

本项目优先考虑课程答辩和可解释性，而不是商业项目级的复杂度。

---

## 2. 技术栈

### 前端

使用微信小程序原生开发：

- JavaScript
- WXML
- WXSS
- JSON

禁止使用：

- Vue
- React
- uni-app
- Taro
- 其他前端框架

### 后端

使用：

- Python
- FastAPI
- SQLAlchemy
- Pydantic

### 数据库

使用：

- MySQL

### 前后端通信

使用：

- HTTP
- JSON
- REST 风格 API

---

## 3. 总体架构

系统采用前后端分离架构。

整体数据流：

微信小程序
    ↓
HTTP / JSON
    ↓
FastAPI
    ↓
业务处理
    ↓
SQLAlchemy
    ↓
MySQL

项目主要分为：

### frontend

负责：

- 页面展示
- 用户交互
- 购物车状态
- 调用后端 API

### backend

负责：

- 接收 HTTP 请求
- 参数校验
- 业务逻辑
- 数据库操作
- 返回 API 响应

### database

负责：

- 数据库初始化
- 数据表设计
- 测试数据

### docs

负责：

- 项目说明
- 系统架构
- API 接口文档
- 数据库设计说明

---

## 4. 后端架构

后端采用简单的分层结构：

API 层
↓
Service 层
↓
Model / Database 层

### API 层

负责：

- 接收请求
- 参数校验
- 调用业务层
- 返回响应

不要在 API 层堆积大量业务逻辑。

### Service 层

负责：

- 点餐业务
- 订单业务
- 菜品相关业务

业务逻辑主要放在这一层。

### Model / Database 层

负责：

- 数据库模型
- 数据库查询
- 数据库持久化

---

## 5. 前端架构

前端保持简单。

主要包含：

### 页面层

负责：

- 页面展示
- 用户操作
- 页面状态

### Components

只在确实存在复用需求时使用。

不要为了“组件化”而创建大量组件。

### Utils

用于：

- API 请求封装
- 简单工具函数

---

## 6. 核心业务

系统只需要支持最基本的点餐流程：

查看菜品
↓
选择菜品
↓
加入购物车
↓
修改购物车
↓
提交订单
↓
查看订单

不需要扩展复杂业务。

---

## 7. 数据库设计

数据库保持简单。

核心实体包括：

- 用户
- 菜品
- 订单
- 订单明细

核心关系：

用户
    ↓
订单
    ↓
订单明细
    ↓
菜品

不要为了扩展性设计大量暂时没有用途的数据表。

---

## 8. API 设计

API 采用简单、直观的 REST 风格。

接口按照业务资源划分，例如：

- 菜品相关接口
- 订单相关接口
- 用户相关接口

接口必须做到：

- 路径清晰
- 请求参数明确
- 返回数据明确
- 错误信息明确

FastAPI 自动生成的 Swagger 文档必须可以正常使用。

API 文档必须与实际代码保持一致。

---

## 9. 功能边界

除非用户明确要求，否则禁止主动增加以下功能：

- 支付
- 微信登录
- OAuth
- JWT 复杂权限体系
- Redis
- 消息队列
- WebSocket
- 推荐系统
- 优惠券
- 积分
- 秒杀
- 库存锁定
- 微服务
- 分布式架构
- 管理后台
- Docker
- CI/CD
- 复杂缓存
- 复杂日志系统
- 监控系统

本项目不追求“大而全”。

---

## 10. 代码设计原则

始终遵循：

简单 > 复杂

清晰 > 高级

可解释 > 炫技

稳定 > 扩展性

课程答辩 > 商业化

禁止为了体现“工程能力”而引入不必要的设计模式和抽象。

例如：

不要随意增加：

- Repository 层
- Factory
- Strategy
- Manager
- BaseService
- BaseRepository
- Generic CRUD
- 大量工具类

只有在确实解决实际问题时才进行抽象。

---

## 11. 文件和目录原则

目录按照“职责”划分，而不是按照“功能越多越好”划分。

新增文件前先判断：

“这个文件是否真的有独立职责？”

如果可以合理放在已有模块中，就不要创建新的目录和文件。

禁止随意改变已有项目架构。

---

## 12. Agent 开发原则

Agent 在修改项目之前必须先理解当前架构。

Agent 不得擅自：

- 修改技术栈
- 修改整体架构
- 大规模重构
- 删除已有核心功能
- 引入复杂依赖
- 扩展需求范围

实现功能时优先使用项目现有结构。

---

## 13. 开发顺序

建议按照以下顺序开发：

1. 初始化项目
2. 确定数据库结构
3. 完成后端基础架构
4. 完成菜品相关 API
5. 完成订单相关 API
6. 完成微信小程序页面
7. 前后端联调
8. 完成接口文档
9. 完成完整流程测试

不要一开始同时实现所有功能。

---

## 14. 最低完成标准

项目至少能够完整演示：

微信小程序
↓
查看菜品
↓
加入购物车
↓
提交订单
↓
FastAPI 接收请求
↓
MySQL 保存数据
↓
前端查询并展示订单

能够完整走通以上流程，即达到项目核心目标。

---

## 15. 答辩原则

所有技术设计必须能够被课程答辩清楚解释。

答辩时应能够解释：

1. 前端负责什么
2. 后端负责什么
3. API 为什么存在
4. Service 层负责什么
5. 数据库保存什么
6. 前端如何调用后端
7. 后端如何操作数据库
8. 一个订单从提交到保存经历了什么

如果一个技术会明显增加解释成本，但对本项目没有实际价值，则不要使用。

---

## 16. 修改后的要求

每次完成一个较大的开发任务后，Agent 需要说明：

- 修改了哪些部分
- 为什么修改
- 对整体架构有什么影响
- 如何运行
- 如何测试

不要只说明“代码已完成”。

---

## 17. 最重要的原则

本项目不是为了展示复杂技术。

而是为了展示：

“一个完整的前后端项目是如何工作的。”

因此始终保持：

功能少
结构清晰
代码简单
数据流明确
接口明确
容易运行
容易答辩